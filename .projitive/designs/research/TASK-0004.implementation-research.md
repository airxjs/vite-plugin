# TASK-0004 Implementation Research Brief

## Task
完善插件使用文档与演进说明

## Summary
为 Airx + Vite 的接入方式、约束和后续演进路径补齐文档。

📋 产出物：
1. Plugin Integration Guide（插件接入指南）
2. Configuration Reference（配置参考）
3. API Documentation（API 文档）
4. Migration & Evolution Guide（迭代路线图）

---

## Design Guidelines and Specs

### Core Design Files

1. **vite-support-policy.md** - Vite 支持策略
   - 插件当前版本：`0.2.0`
   - peerDependency：`vite@^5.0.0`
   - JSX 注入机制：esbuild `jsxInject`, `jsxFactory`, `jsxFragment`
   - 支持范围：Vite 5.x (Stable), Vite 6.x (Candidate), Vite 7.x (Experimental)

2. **release-verification-baseline.md** - 发布验证基线
   - 最小示例：`examples/minimal/`
   - 构建门禁、质量门禁、发布门禁定义
   - README 增强建议已在文档中

3. **designs/core/architecture.md** - 插件架构文档（TASK-0006 产出）
   - 模块结构、API 边界
   - 与 Vite/Airx 的耦合点

4. **designs/core/style-guide.md** - 代码风格指南（TASK-0006 产出）
   - TypeScript 风格、API 设计原则

### README.md 当前状态

当前 README 只有 `# vite-plugin`，几乎无内容。

---

## Code Architecture and Implementation Findings

### source/index.ts#L1-L16
插件当前为最小实现，无配置选项：

```typescript
export function VitePluginAirx(): Plugin {
  return {
    name: 'airx',
    config(config: UserConfig, env: { mode: string, command: string }) {
      config.esbuild = {
        ...config.esbuild,
        jsx: 'transform',
        jsxFragment: '__airx__.Fragment',
        jsxFactory: '__airx__.createElement',
        jsxInject: "import * as __airx__ from 'airx'"
      }
      return config
    },
  }
}
```

### 缺失文档分析

| 缺失文档 | 当前状态 | 需要新增 |
|---------|---------|---------|
| README.md | 仅 `# vite-plugin` | 需要完整的接入指南 |
| Plugin Integration Guide | 不存在 | 需要文档说明接入步骤 |
| Configuration Reference | 插件无配置项 | 文档说明为何无配置 |
| Migration & Evolution Guide | 不存在 | 需要说明版本演进路径 |

---

## Implementation Plan

### 1. Plugin Integration Guide

在 `README.md` 中补充：

```markdown
# vite-plugin-airx

Vite 插件，用于将 Airx JSX 转换接入 Vite 项目。

## 安装

```bash
npm install vite-plugin-airx
```

## 快速开始

### 1. 安装依赖

```bash
npm install airx vite-plugin-airx
```

### 2. 配置 vite.config.ts

```typescript
import { defineConfig } from 'vite'
import airx from 'vite-plugin-airx'

export default defineConfig({
  plugins: [airx()]
})
```

### 3. 编写组件

```tsx
import * as airx from 'airx'

function App() {
  return () => airx.createElement('div', null, 'Hello Airx!')
}

airx.createApp(airx.createElement(App)).mount(
  document.getElementById('root')!
)
```

## Vite 版本支持

| 版本 | 支持状态 | 说明 |
|------|---------|------|
| Vite 5.x | ✅ 官方支持 | peerDependency 明确声明 |
| Vite 6.x | ⚠️ 待验证 | 手动验证通过后可声明 |
| Vite 7.x | ⚠️ 待验证 | 需主站配合验证 |

## 系统限制

- 插件依赖 `airx` 包导出 `Fragment` 和 `createElement`
- 暂不支持 SSR 场景
- 不与其他 JSX 插件（如 `@vitejs/plugin-react`）同时使用
```

### 2. Configuration Reference

由于插件当前无配置选项，文档说明：

```markdown
## 配置参考

### 当前版本

`vite-plugin-airx` 当前版本（v0.2.0）为零配置设计，无需任何配置选项即可使用。

### 未来版本配置项（计划中）

未来版本可能支持以下配置项：

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `jsxFactory` | `string` | `'__airx__.createElement'` | JSX 工厂函数名 |
| `jsxFragment` | `string` | `'__airx__.Fragment'` | Fragment 函数名 |
| `importSource` | `string` | `'airx'` | JSX 运行时导入路径 |
```

### 3. Migration & Evolution Guide

在 `README.md` 中补充演进说明：

```markdown
## 演进路线

### v0.2.x（当前）

- 零配置插件
- 支持 Vite 5.x
- 最小 JSX 注入

### v0.3.x（计划中）

- 支持 Vite 6.x
- 配置选项扩展
- 完整的 CI 验证

### v1.0.0（计划中）

- 移除 Vite 5.x 支持
- 稳定 API
- 完整的文档和示例

## 升级指南

### 从 v0.1.x 升级到 v0.2.x

v0.2.x 保持向后兼容，升级步骤：

```bash
npm install vite-plugin-airx@latest
```
```

---

## Risks and Open Questions

### Known Risks
1. **文档与实现同步**：插件未来增加配置项时，README 需要同步更新
2. **Vite 6/7 支持声明**：在未经验证前，不应在文档中宣称支持

### Assumptions
1. 插件保持零配置体验一段时间
2. 主站会逐步考虑迁移到本插件

### Unresolved Questions
1. 是否需要 CHANGELOG.md 来记录版本变更历史？
2. 插件的 API 稳定性保证是什么？
