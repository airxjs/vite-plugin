# TASK-0012 Implementation Research Brief

- Task: 为 vite-plugin-airx 添加 ESLint 配置
- Summary: 为 vite-plugin-airx 添加与 router 一致的 ESLint 配置，包括 .eslintrc.cjs、lint script 和必要的 devDependencies。公开发布的 npm 包需要 lint 门禁保证代码质量。
- Status: EXECUTED

## Design Guidelines and Specs

- airxjs/router/.eslintrc.cjs - 参考配置（完全复用）
- airxjs/router/package.json#L7 - lint script 定义
- airxjs/router/package.json#L23-L26 - eslint devDependencies

## Code Architecture and Implementation Findings

### 当前状态
- vite-plugin-airx 是公开发布的 npm 包（vite-plugin-airx）
- 无 ESLint 配置，无 lint script
- package.json 中只有 typescript 作为 devDependency

### 实施方案
1. 复制 router 的 .eslintrc.cjs 配置
2. 在 package.json 添加 lint script: `eslint source`
3. 添加 eslint devDependencies:
   - @typescript-eslint/eslint-plugin: ^5.59.9
   - @typescript-eslint/parser: ^5.59.9
   - eslint: ^8.42.0

## Implementation Plan

### 变更文件
1. `package.json` - 添加 lint script 和 eslint devDependencies
2. `.eslintrc.cjs` - 新建 ESLint 配置文件

### 验证
- `pnpm run lint` 无错误输出
- `pnpm install` 正常安装依赖

## Risks and Open Questions

- 无风险：纯配置添加，不影响现有代码逻辑
- 构建类型错误（TS4058）为预先存在的问题，与本次变更无关
